# Dulo — pre-submission review, Monday 14 Sep 2026

Findings from seven review lenses (requirements, concept, QA, resilience, code, story and verification passes) run against HEAD `0460024` on the local dev server. Deadline Fri 18 Sep 16:00 ET (21:00 Irish). Judging runs to 2 Oct, so the live demo must survive two weekends with US markets closed. The judging question: "could this be a real app that people will actually use?" — real user problem, working end-to-end demonstration, why Solana is necessary, execution quality.

> Internal planning notes are kept privately. This tracked copy keeps the H/M/L findings below because code comments and tests cite them by id (H2, M6, L10, ...); section numbering is unchanged. Where a finding's proposed copy or setting was superseded on 15 Sep it says so inline.

---

## 6. Confirmed findings by severity

Severities below are the *adjusted* ones after verification (several lenses over- or under-called). File paths are relative to the repo root. Lens tags show who raised it.

### HIGH — would cost the win or break the demo

**H1. No GitHub remote, no deployment, no keys, no production DB** *(requirements; verified)*
`git remote -v` is empty, branch is `master`, `github.com/dulofun` is free. No Vercel project, Supabase, Helius/Jupiter/Pyth keys, `SERVER_WALLET_SECRET` empty. Tracked files are clean of secrets (audited: no keys, no `.env*`, bot wallets are `Keypair.fromSeed(sha256(handle))`, unfunded, excluded from rollover points). **Fix (Founder, ~3 h Tue):** push now (history is fine, judges see HEAD at judging time — do not wait for the README); Supabase with `DATABASE_URL` = transaction pooler 6543 + `?pgbouncer=true`, `DIRECT_URL` = **session pooler 5432** (the `db.<ref>.supabase.co` host is IPv6-only on Free and `prisma db push` fails from an IPv4 laptop); *(superseded 15 Sep: Supabase in us-east-1 and `DATABASE_URL` = transaction pooler 6543 with `?pgbouncer=true&connection_limit=1&connect_timeout=5`, see HANDOFF §4.3)*; Vercel project with every env var; `npx prisma db push && npx prisma db seed` from the laptop; tick with the Bearer header; incognito Phantom test. The `.claude/worktrees/...` branch is excluded via `.git/info/exclude` and a single-branch push ignores it — do not remove it while the other session may resume.

**H2. Bots earn Play points and fill the Season leaderboard** *(qa, resilience; reproduced)*
`src/lib/cron/evaluate.ts:488` evaluates every User; `loadInternalEvents` (`:237`) turns the 3–8 seeded `LeagueTrade` rows per bot into `league_trade` events, so Scout (`catalogue.ts:120`) completes for all 15. Live: `/api/v1/leaderboard` = 15 bots at rank 1 with 50 pts; `/api/v1/partners` shows xStocks "completions: 15" with zero real users; `/api/v1/mirror` lists bots with `isBot:false` hard-coded at `queries.ts:700`. Contradicts `league.ts:606` ("Points are for real users only") and the BOT chip title. **Fix (Claude, ~1.5–2 h):** filter bots in `evaluateAllUsers` and `snapshotAllWallets` (`snapshot.ts:128`) with `user: { leagueAccounts: { none: { isBot: true } } }` — no migration needed; put the same exclusion in the `groupBy` where-clause at `queries.ts:331` (not the post-`take` user lookup, which leaves holes), and in `getUserProfile` rank (`:473`), `seasonRankOf` (`:590`), `completionsByPlayKey` (`:278`); drop the hard-coded `isBot:false` at `:700`; one-off delete of the 15 `play:scout` PointsEvents + bot PlayProgress locally (prod is fresh — deploy before seeding); update `tests/cron.test.ts:243,556`; add a vitest case. Do not keep bots on the board with a chip — it still contradicts "never scores".

**H3. Mirror page for a League leader goes empty once its wallet has any snapshot** *(qa; reproduced)*
`GET /api/v1/mirror/DE8jfH1j…` returns `source:"snapshot", legs:[], totalUsd:0`. `queries.ts:642` takes the snapshot branch on any row without checking `allocation.legs.length`, so the paper fallback (`:659-678`, documented in HANDOFF §3.4) is unreachable after the first tick. `queries.ts:698` gates the "on-chain" list on snapshot count > 0, so bots leak in. Every leaderboard row's Mirror link lands on "$0.00 across 0 legs". **Fix (Claude, ~1 h, part of H2):** snapshot branch only when `legs.length > 0 || accounts.length === 0`, else paper; gate `listMirrorTargets` on a non-empty latest snapshot; add a fixture test (`tests/mirror.test.ts` already mocks `@/lib/server/db`); add `holdings.length > 0` guard.

**H4. Friday rollover creates next week's League without bots** *(concept, resilience; verified)*
`league.ts:591-620` `rollover()` calls `ensureLeague` (`:615`) but never `seedBots`; `tickLeague` (`:634-645`) only ensures. `seedBots` is called solely from `prisma/seed-league.ts:26`. From Fri 18 Sep 20:00 UTC `/league` renders "Nobody has traded this week yet." (`league/page.tsx:106-111`), again from 25 Sep. Only the "Last week" block shows the settled bots. **Fix (Claude, ~1.5 h):** in `tickLeague` right after `ensureLeague`, `leagueAccount.count({ leagueId, isBot: true }) === 0` → `seedBots(client, leagueId, now)` in its own try/catch (self-heals on the next tick; also covers a fresh deploy where the cron creates the League before the seed). Cosmetic: bot trades seeded at rollover carry Friday timestamps; never displayed. Test: tick with an empty open League → 15 bot accounts; second tick → no-op (reuse mocks at `tests/league.test.ts:519-545`, add `mocks.db.leagueAccount.count`).

**H5. Nothing opens next week's Calls markets after Friday settlement** *(concept, resilience; verified)*
`calls.ts:564-586` `tick()` only settles; `createMarket` (`:376`) has one caller, `prisma/seed-calls.ts:43`. The seeded NVDA/TSLA/SPY lock Fri 20:00 UTC = 16:00 ET, the deadline. `getCallsBoard` returns every Season market, so `/calls` shows three settled cards, never the empty state, and the footer says "Every market is locked; payouts land in your ledger after the close" (`calls/page.tsx:166`) forever. The Oracle Play becomes uncompletable. **Fix (Claude, ~2.5 h):** `ensureWeeklyMarkets(seasonId, now)` at the top of `calls.tick` for `SEED_CALL_TICKERS`: if no Market with `settleAt = fridaySettleAt(now)`, create with strike = `mondayOpenStrike` (skip, do not fall back to fixed strikes, when no quote — the next tick retries); only when `now < locksAt(settleAt)`; `seed-calls.ts` delegates. Add `@@unique([seasonId, ticker, settleAt])` to `Market` (`schema.prisma:203-218` has only an index) with P2002/`skipDuplicates` handling. Label a weekend-created strike "price when the market opened", not "Monday open". Footer for the all-settled case: "Next week's markets open Monday". Tests: Fri 18 Sep 20:06 UTC and Sat 19 Sep → three markets for Fri 25 Sep 20:05 UTC, idempotent.

**H6. README is the create-next-app default; no LICENSE; no reused-code disclosure** *(all lenses; verified)*
`README.md` is the verbatim template from `ef85150`; no `LICENSE`; `package.json` has no `license`; `docs/screenshots/` and `src/app/admin/` do not exist. Hackathon rules require only "at least one link" and "original work, OSS components fine if you say so", so MIT is the project's own checklist choice — but the README is the one place the disclosure and the why-Solana argument reach a judge who opens the GitHub link. A strong hackathon README has a one-liner, screenshot, a "where Solana is load-bearing" section, dev instructions, a live link and a licence. **Fix (Claude, 3 h Thu; LICENSE 0.1 h Tue):** README outline: (1) title, one-liner, links row (live · video · hackathon · MIT); (2) three-sentence what-it-is + four-row judging table; (3) screenshots from `docs/screenshots/`; (4) the five mechanics in the binding vocabulary; (5) "How a Play verifies" sequence mermaid: cron → `/api/cron/tick` → ChainAdapter (Helius `getParsedTokenAccountsByOwner`, Token-2022, ScaledUiAmount multiplier) → AssetSource → PriceSource (source + age) → Snapshot → `lib/plays/engine.ts` (7 rule types: `hold_any`, `hold_consecutive`, `net_increase_days`, `hold_through_date`, `diversified`, `mirror_match`, `internal_event`) → PlayProgress (sticky completion) + append-only PointsEvent → Badge queue → Token-2022 mint; include the `diversified` rule JSON from `catalogue.ts:61-68` and "Adding a Play is a DB row, not code"; (6) architecture flowchart (PWA → `/api/v1` zod handlers → `lib/server/queries` → Prisma; `lib/core` interfaces at `types.ts:83-137`; CAIP-19; N wallets); (7) why Solana; (8) points-only framing + cut list; (9) partners: how a listing works today, `/admin/attribution` (only if built), mailto; (10) run locally: Node 20+, Postgres, `cp .env.example .env.local` (requires the `.gitignore` fix), `npm i`, `npx prisma db push`, `npm run db:seed` (idempotent: Season 0, 5 Partners, 12 Plays, this week's League + 15 bots, 3 Calls — the header comment at `seed.ts:17` saying it does not create bots is stale), `npm run dev`, tick curl with Bearer, `npm test`/`typecheck`/`lint` (run `npm run dev` or `npx next typegen` before `typecheck` on a fresh clone because `next-env.d.ts` is gitignored); (11) env table from `src/lib/server/env.ts:7-21` + `NEXT_PUBLIC_APP_NAME` from `src/lib/config.ts:2`, pgbouncer note; (12) deploy; (13) known limitations (Pyth key, earnings calendar, Mirror 7d/30d not cash-flow adjusted, badge mint until first run); (14) disclosure — **wording only after Founder confirms**: the repo shows no evidence of ported code (HANDOFF `<path>` placeholders never filled, no references to earlier projects in src, all commits 14 Sep) — if true: "design lineage from the author's earlier projects; fresh TypeScript implementation; third-party OSS under their own permissive licenses (MIT, Apache-2.0, ISC, Unlicense); data from xStocks API, Jupiter Price v3, Pyth Hermes, Helius; partner marks are their owners'; no Meteora DBC"; (15) MIT. Also `LICENSE` file at root (GitHub keys off the file) and `"license": "MIT"`.

**H7. 2-minute demo video not recorded** *(requirements, story)* *(superseded 15 Sep: the form takes a Pitch Video ≤ 3:00 and a separate Technical Video ≤ 5:00; scripts in HANDOFF §6 and §6b; no app or entrant counts in any script)*
Optional per the rules, but the only way a judge without xStocks sees the differentiating loop and the only artefact immune to infra failure over two weeks. **Fix (Founder, 3 h Thu 14:00–17:00 Irish when League and markets are open):** rehearse locally (public RPC fallback works for the First Position beat), final take on the deployed URL, ≤ 2:00, hook inside 10 s (HANDOFF §6's opener runs to 0:15 — trim), unlisted YouTube. Two beats need a plan B decided Wednesday night: 1:45 "Badge lands in wallet" (fund + mint once, else show the "Minting soon" tile and say "queued for the server wallet") and 1:15 "Pyth settles" (say "settled from the Friday close, source printed on the card" — see M6). Revised shot list with URLs and DB preconditions is in the story lens and goes into HANDOFF §6 on Tuesday (T6); preconditions: demo wallet holds ≥ $5 xStock on prod, cron ticking since Tue, ≥ 2 prior League trades, spendable points ≥ 10 for the Call, a Mirror target with ≥ 2 legs that is not your own wallet. Drop "Thirty apps" (never quote an app count); "Built solo in 4 days" is defensible.

### MEDIUM — judges would notice

**M1. `vercel.json` `*/5 * * * *` fails a Hobby deployment; a naive daily schedule can void Friday's Calls** *(requirements, resilience, code; docs verified today)*
Hobby = once per day, ±59 min, "Hobby accounts are limited to daily cron jobs" at deploy. Hobby with Fluid compute allows `maxDuration` 300 (the "60s is the Hobby-plan ceiling" comment at `tick/route.ts:8` is stale). A daily `0 6 * * *` would settle Calls up to ~25 h late and can hit `VOID_AFTER_MS` = 24 h (`calls.ts:64`), refunding every stake on both judging Fridays. What a daily-only tick actually degrades: Friday settle/rollover timing and badge minting; sign-in, Plays refresh, Mirror verify and League reads already run their own evaluation (`auth/verify/route.ts:197`, `plays/refresh/route.ts:44`, `mirror/record/route.ts:65`, `league-views.ts:204-207`). **Fix (both, ~1.5 h):** recommended: keep `*/5`. Otherwise: `vercel.json` → `10 20 * * *` (fires 20:10–21:09 UTC, inside the void window, covers settle + rollover) plus `.github/workflows/tick.yml` on `*/5` with `curl -fsS --max-time 120 -H "Authorization: Bearer $CRON_SECRET" https://<app>/api/cron/tick | jq -e '.data.steps | all(.ok)'` (Actions is often 5–30 min late; the daily Vercel entry stays the Friday backstop; cron-job.org has a 30 s client timeout). Pingers use the header only — `?secret=` at `api.ts:88-93` would land the secret in request logs; disable it when `NODE_ENV=production`. Raise `maxDuration` to 300, fix the comment, fix the route doc at `:12/:32` to list all four steps.

**M2. Cron tick 76–88 s on the public RPC; 8–12 of 16 wallets 429; bots snapshotted** *(qa; reproduced)*
`HELIUS_API_KEY` empty → `env.ts:40` falls back to `api.mainnet-beta`; the real cost per 429'd wallet (~45 s) is web3.js's built-in 429 retry (5 retries, 500 ms doubling to 8 s) because `solana.ts:297` builds `Connection` without `disableRetryOnRateLimit: true` — dropping the adapter's own attempts barely helps. Bots cost 30 RPC calls + 15 empty rows per tick. `runTick` runs snapshot first, so a slow RPC starves games/badges at `maxDuration` 60. **Fix (Claude, ~1 h + the founder's key):** `disableRetryOnRateLimit: true`; bot exclusion (H2); `maxDuration` 300; reorder to games → snapshot → evaluate → badges (league/calls/badges never read Snapshot; HANDOFF's "badges after games" rule holds; only `tests/cron.test.ts:505` pins the order) or give snapshot a ~40 s budget that reports skipped wallets. With Helius Free (1M credits, 10 RPS) 32 calls/tick finish in seconds; the free tier covers ~90 wallets for the whole window once bots are out.

**M3. Placeholder partners "Stocklana Builder #1/#2 · Listing pending"** *(requirements, concept, qa, story; verified)* *(superseded 15 Sep: both placeholders and their Plays deleted, the seed prunes them from existing DBs, chips read "Plays live" / "Coming soon")*
`partners.ts:111-134` (`logoUrl null`, `links {}`, `placeholder: true` — a flag no query or component reads), `catalogue.ts:157-178` ("Try Stocklana Builder #1/#2", coming soon). Live on `/partners` (2 of 5 cards as "SB" initials), `/plays` (last two groups), and `/partners/stocklana-builder-1` where `PartnerHeader.tsx:41-46` stamps "Listed on Dulo" above "Listing pending". Not on the landing today, but the P5 partner row would inherit them. Real Stocklana apps can replace them only with their consent (read-only listing, their logo and link). **Fix (Claude 1.5 h Wed; Founder DMs Tue):** replace with real name/logo/link/blurb and one coming-soon Play naming the real action (read-only, with consent, matching HANDOFF's own fallback), or delete both. Give real partners real slugs (`partners/[slug]/page.tsx` derives the tab title from the slug). Update `tests/rules.test.ts:287-316`. The seed is upsert-only with no prune: locally `prisma.partner.delete({ where: { slug } })` cascades Campaign → Play → PlayProgress; prod is fresh so decide before the first prod seed. Expose `placeholder` through `toPartnerSummary` (`queries.ts:107-114`) so PartnerHeader/PartnerCard drop the "Listed on Dulo" chip for pending listings. Do not build a Jupiter-Trigger-verified Play (needs a new event source + key, 4–6 h).

**M4. Zero partner Plays verify** *(concept; verified)*
All four partner Plays are `comingSoon` with `partnerAssetIds: []` (`catalogue.ts:135-178`); `engine.ts:757` returns `partner_pending`; `seed.ts:156` writes `isActive=false`. Structural: `snapshot.ts:70-71` filters to the xStocks mint set and Kamino deposits create an obligation account, not a wallet token, so `hold_any` can never verify Kamino; an adapter is required. Verified live: `GET https://api.kamino.finance/kamino-market/5wJeMrUYECGq41fxRESKALVcHnNX26TAWy4W98yULsua/users/{wallet}/obligations` is public and keyless (xStocks Market; "Sentora xStocks Market" `8BNUWRSibVasaAmhYpBCFpGgMisGKfVAf9ho3Cmf6vjr` also exists). No new rule type needed: `rules.ts:66` accepts any snake_case event, so `kamino_collateral` = `internal_event "kamino_deposit" count 1` plus a never-throws `partnerEventsForUser` loader merged at `evaluate.ts:281-298` (pattern of `src/lib/mirror/events.ts`). Jupiter Recurring is not viable (key required, API marked unmaintained). **Fix (should, Claude ~4 h + Founder one real SPYx/QQQx deposit ~0.5 h):** only if Tue/Wed run ahead. Fallback (0.5 h): move the on-chain-verified Mirror Play to the Jupiter campaign so Jupiter has one verifying Play.

**M5. The only on-chain write (soulbound Badge mint) has never run on mainnet** *(concept; verified)*
`SERVER_WALLET_SECRET` empty; `tests/badges.test.ts:173` fakes the send. Cannot break the demo (`badges.ts:112-121` skips; profile shows "Minting soon") but any judge with $5 of an xStock queues a badge that reads "Minting soon" for two weeks, and the video beat cannot be recorded honestly. The exact 8-instruction transaction was compiled offline for all five badges: 723–741 bytes (limit 1232), `mintLen` 238 + TLV 187–214 B; ~0.006 SOL per badge; 0.3 SOL is a safe two-week buffer. Operational risk: `maxDuration` 60 with `DEFAULT_MINT_LIMIT` 5 (`badges.ts:29`) — five confirmed sends after snapshot/evaluate/games can exceed the budget, and a kill between confirm and `db.badge.update` re-mints that row (duplicate soulbound token); `sendAndConfirmTransaction` (`mint.ts:200`) has no timeout; the queue takes the oldest 5 pending rows so permanently failing rows starve others; the metadata URI uses `NEXT_PUBLIC_APP_URL` (`mint.ts:91-93`) so a prod deploy left at localhost mints localhost URIs. **Fix (Founder 0.75 h + Claude 0.25 h, Wed):** `solana-keygen new`, fund 0.3 SOL, set `SERVER_WALLET_SECRET` + `HELIUS_API_KEY` + correct `NEXT_PUBLIC_APP_URL` on Vercel, `GET /api/cron/tick?steps=badges` with one pending row, verify on Solscan, link in README/video; drop `DEFAULT_MINT_LIMIT` to 2; pin `@solana/spl-token-metadata@0.1.6` (`npm i`, Founder).

**M6. Copy drift: "real Jupiter swaps" and "settled from Pyth"** *(story, requirements, concept, qa; verified)*
Mirror: `catalogue.ts:107` ("real Jupiter swaps", rendered on `/plays` and `/partners/xstocks`), `partners.ts:86` ("Mirror legs execute through Jupiter Swap" — the execution API the cut list forbids, on `/partners` and `/partners/jupiter`), `badges/keys.ts:61` (served by `metadata.json`, `/api/v1/badges/me`, profile tooltip; not baked on-chain — `mint.ts:101-104` writes only name/symbol/uri/badge/season). Pyth: `page.tsx:39` ("settled from Pyth") and `catalogue.ts:128` ("Points only, settled by Pyth.") while `calls/page.tsx:103` already says "Pyth, Jupiter as fallback, source shown on every card" and `MarketCard.tsx:113` prints "Settled from Jupiter". Both Hermes hosts return 401 without a key today; **a key would not change settlement**: Hermes metadata shows `Equity.US.NVDA/USD` schedule `America/New_York;0930-1600`, `settleAt` = close + 5 min (`calls.ts:171-172`) and `calls.ts:465` only accepts a Pyth publish ≥ `settleAt`, so the Pyth branch is dead by construction. HANDOFF: lines 23 and 71 say real swaps, 258 (P4 prompt) specifies Swap v2 execution, 60 says code was ported, 3 says paste §0 verbatim into CLAUDE.md (would regress it); CLAUDE.md's last line frames deep links as a "degrade". **Fix (Claude, 0.75 h Tue):** three Mirror strings → "Mirror opens one prefilled Jupiter swap per leg from your own wallet; land within 20% of the allocation and the next snapshot verifies it"; two Pyth strings → "settled from the Friday close, source shown on the card"; HANDOFF: 3-line status banner at the top ("pre-build plan kept for transparency; README is authoritative; Mirror shipped as deep links by decision; no code was ported") + fix 23/60/71/258 + soften line 3; CLAUDE.md last line → the decision, not a degrade; re-run `npm run db:seed` (upserts desc/blurb). Do not buy a Pyth key.

**M7. Pitch claims "ScaledUI multipliers make dividend-aware Plays possible" but no dividend Play exists** *(concept; verified)*
HANDOFF §2 line 72; no rule reads the multiplier (`rules.ts:55` `CALENDAR_KEYS=["earnings"]`, `engine.ts:737` only passes it through). Nothing judge-facing makes the claim yet. The mechanism is real: xStocks docs say dividends reinvest as a multiplier increase activated at 00:30 UTC the day after ex-date; a live scan of 832 xStocks shows 24 assets (KOx 1.01833 → 1.02256, MRKx, TMOx, MOx, GILDx, PSAx, CCIx, …) with reason "Dividend" activating 2026-09-15 00:30 UTC and 347/832 already > 1. `Snapshot.holdings[].multiplier` is already stored (`snapshot.ts:89-97`). **Fix:** must (0.1 h): drop or reword the claim when writing README/video to "multiplier-correct holdings (splits and dividends normalised)". Should (Claude ~3 h): `multiplier_increase { minUsd }` rule — complete when an asset held ≥ minUsd across two snapshots has a strictly higher multiplier in the later one (proof: assetId, old/new multiplier, timestamps); guard the `solana.ts:663` returns-1-when-unreadable case (require the earlier multiplier observed in two consecutive snapshots; ignore 1 → x), cap at < 5% or name it neutrally since splits also raise it; `rules.ts` schema + `PLAY_RULE_TYPES` + `ruleToHint` (exhaustive switch), evaluator modelled on `evalHoldThroughDate` (`engine.ts:411`), fixtures, catalogue entry "Dividend Collector" 350 pts under xStocks. Completion needs a "before" snapshot on prod: SPYx after its Fri 18 Sep ex-date would complete during judging, not in the video — show it in progress with the scheduled `newMultiplier` as proof.

**M8. Landing is thin; Partners and Mirror are unreachable from the nav** *(qa, story, concept)*
`src/app/page.tsx` (117 lines) = hero + three tiles + footer line. No problem statement, no why-Solana, no partner row, no stats, no Mirror/Badges mention, no list-your-project CTA, no footer links. `nav.ts:12-15` has only Plays/League/Calls/Leaderboard; `/partners` is two hops away via `/plays` group headers; `/mirror` only via a leaderboard row; the 500-pt Mirror Play card has no link to `/mirror`. Scheduled P5 work, but the live URL is the first thing a judge opens. **Fix (Claude, 3.5 h Wed):** hero sub-headline with problem + answer (Dulo verifies what your wallet already does — holding, diversifying, DCA — and turns it into Plays, a weekly League, Calls and soulbound Badges; *superseded 15 Sep: no app counts and no claim about other apps' retention*); "Watch the 2-min demo" ghost link gated on an env var; four-tile why-Solana section (*superseded 15 Sep: the heading is "Why it works on Solana", no exclusivity*) (public Token-2022 holdings → no broker API; every price carries source + age, Pyth-first with Jupiter fallback — **not** "Pyth when open"; Mirror = one prefilled Jupiter swap per leg, you sign; Badges are NonTransferable Token-2022 — keep "some mint a Badge" phrasing until M5 lands); mechanics grid extended to Mirror (`/mirror`, "Deep links") and Badges (`/profile`, "Soulbound"); partner row reusing `PartnerCard` from `/api/v1/partners` filtered to `logoUrl != null` (plain `<img>` — external hosts, no `next/image` config needed) + dashed "Your project here" → mailto; "List a Play" CTA (`NEXT_PUBLIC_PARTNER_EMAIL` in `config.ts` + `.env.example`); footer (GitHub, X, "Built for the Solana Stocklana hackathon · Points only. No cash value. Onboarding, not churn."); `NAV_ITEMS` + Mirror and Partners with an explicit `MOBILE_TABS` (it currently spreads `NAV_ITEMS`; `MobileTabBar.tsx:16` is `grid-cols-5`); "Go to Mirror" CTA on the Mirror Play card. **Skip live wallet/points counters** — they would print 0–1 for two weeks; structural counts only (12 Plays, 5 Partners, 3 Calls, League pool). Put the xPoints contrast in README/video, not the landing.

**M9. League and Mirror tables overflow at 375 px; no scroll affordance** *(qa; measured)*
`/league` container 341 vs scrollWidth 392; Mirror allocation 341 vs 391; plan table 309 vs 367 — header cut to "Equity | R", "+4.16%" to "+4", "Open in Jupiter" to "Open in J…"; the PriceChip in `AllocationTable.tsx:49` is squeezed to 88 px and wraps into four lines under the hint "Every price carries its source and age". Root cause: `ui/table.tsx:10` `overflow-x-auto` + `:72/:85` `whitespace-nowrap` on every cell; overlay scrollbars paint nothing. Every common phone (390, 412) overflows on all three. `/leaderboard`, `/calls`, `/partners/*` fit. **Fix (Claude, ~1–1.5 h):** `hidden sm:table-cell` on the Equity th/td at `LeagueLeaderboard.tsx:31/67` and skeleton `:85/:94` (→ 292 ≤ 341); `hidden sm:block` on the 64 px weight bar at `AllocationTable.tsx:41` + `sm:w-[38%]` on `:28` (keeps the Price column, stops the chip wrapping); plan table: drop Weight below `sm` AND drop the " USDC" suffix from cells (header already says USDC) or use icon + "Jupiter" with `sm:` revealing "Open in" — the 4 px margin from dropping Weight alone breaks at a $1,000 budget. `PositionsTable.tsx:35-40, 111-116` (signed-in, six nowrap columns) has the same problem; same pattern.

**M10. All three Call markets show "No stakes yet"** *(qa; verified)*
`seed-calls.ts` only creates markets; `PoolBar.tsx` renders "No stakes yet", "—" multipliers and a 50/50 bar when `odds.total === 0`. A fresh judge wallet has 0 spendable points (no welcome/admin grant anywhere) and `PlaceCallDialog` blanks below `MIN_CALL_POINTS=10`, so the parimutuel mechanic — the interesting part — is never demonstrable to a judge. **Fix (should, Claude ~1.5–2 h):** `seedCallStakes` using `placeCall()` after an admin PointsEvent per bot for exactly the stake (unique ref `admin:seed:<marketId>:<userId>`), so Position PK, refs and `yesPool/noPool` stay consistent; idempotent (skip bots that already hold a Position; re-running `placeCall` appends ":2" and double-stakes); run for existing markets too (`seedCalls` currently `continue`s on existing); tilts e.g. NVDA 320/180, TSLA 150/350, SPY 400/250; hook into `ensureWeeklyMarkets` (H5) so every new week's markets get stakes; one vitest asserting pools = Σ positions. With H2 bots no longer complete Oracle from these. Do not seed a "last week settled" market — Friday settles these for real before judging opens.

**M11. Snapshot rows grow 288/wallet/day with no retention; evaluate re-reads 45 days of holdings JSON per user per tick** *(resilience, code; verified)*
`snapshot.ts:105-111` unconditional insert incl. empty wallets; `evaluate.ts:34, 281-288` loads every row in the window for every user every tick, though every history rule consumes only the last row per UTC day (`dailySnapshots`), the latest row (`hold_any`) or the latest row after an intent (`mirror_match`). Bots alone would push Supabase Free's 5 GB egress by day 9–10; DB size (~20–50 MB by 2 Oct), tick duration and the 8 s sign-in path are fine through 2 Oct with realistic user counts. **The proposed per-wallet dedupe is unsafe as stated:** `bucketSnapshots` (`evaluate.ts:170-186`) merges only wallets present in a bucket, so skipping an unchanged wallet's row drops its holdings from that day-end view for multi-wallet users (breaks `hold_consecutive`, phantom `net_increase_days`); and `mirror_match` needs a row strictly after the intent, so a skipped write can stall a Mirror completion up to 60 min. **Fix (should, Claude ~2–3 h):** H2 bot exclusion (removes 30 RPC calls + 15 rows/tick); skip the insert only when new holdings are `[]` AND the latest row is `[]` and younger than 60 min; read side: holdings only for the last 48 h plus `DISTINCT ON (walletId, date_trunc('day', takenAt))` last row of earlier days; `deleteMany` `takenAt < now − 46 d` at the end of the snapshot step; `connection_limit=5&pool_timeout=20`. Regardless, watch the Supabase usage page.

**M12. League trading is closed Fri 20:00 UTC → Mon 00:00 UTC (52 h/week, both judging weekends) under "trade after hours" copy** *(resilience; verified)*
`league.ts:142-144` `isTradingOpen` requires `now >= weekStart`; `:92` "League closed until Monday 00:00 UTC"; `league/page.tsx:72` and landing `page.tsx:32` say "Trade after hours too". The window closes at the deadline instant; the Scout Play is dead on weekends; ~31% of the judging window and its first impression is a closed League. Deliberate, honest, with countdown and last-week standings, so not high. **Fix (should — product decision for Founder, Claude ~2 h):** `isTradingOpen = status === "open" && now < weekEnd` (drop the lower bound; `currentWeek(now)` already points weekends at next week's League, `ensureLeague` upserts it open, rollover settles only `weekEnd < now`); null `opensIn` at `league-views.ts:80`; update `page.tsx:72`, module header, `LeagueCountdown`/`TradeForm` strings, `tests/league.test.ts:150-153,160,244-248`; needs H4 so the early-opened League is not an empty board.

**M13. Earnings calendar dates are wrong for every in-window reporter** *(resilience; verified on IR pages)*
`src/lib/plays/earnings-2026.json:25-27`: MU 09-23 (actual Wed 30 Sep AC), ACN 09-24 (actual Thu 1 Oct 08:00 ET), NKE 09-29 (actual Thu 1 Oct AC). Holding MUx on 22–23 Sep would award the 400-pt Earnings Holder Play and the gold badge a week before Micron reports, with `proof.earningsDate=2026-09-23` in the drawer — the "why Solana" example (HANDOFF line 72, video 0:35) demonstrably false to any judge who checks. Also missing: LENx (16 Sep AC), AZOx (22 Sep BMO), GISx/CTASx/PAYXx (23 Sep), DRIx (24 Sep BMO); JBLx/STZx/MKCx unverified. None of MUx/ACNx/NKEx is in `XSTOCKS_FALLBACK` (`xstocks.ts:300-331`) so a fallback-served snapshot on D-1 silently fails the Play (moot once the parallel catalogue bundle includes them — confirm). **Fix (Claude, ~1 h Tue):** set MU 2026-09-30, ACN 2026-10-01, NKE 2026-10-01; add LEN/AZO/GIS/CTAS/PAYX/DRI; drop "estimated" from `_note`; vitest asserting the in-window dates; confirm the bundle contains MUx/ACNx/NKEx/AZOx/GISx/CTASx/PAYXx/DRIx/LENx (mints: MUx `XsQLZycSZ7QnBBdBXQaTbQdiUcbRqjNJgyBGAMzhHav`, ACNx `Xs5UJzmCRQ8DWZjskExdSQDnbE6iLkRu2jjrRAB1JSU`, NKEx `XsGYpMvKbVt6ViHqRd7cF3s746dAMFBQWcC49hB9VVP`); proof hint "hold by 29 Sep for MUx, by 30 Sep for NKEx/ACNx" (engine window is D-3..D-1, `engine.ts:465-472`). With correct dates the Play is honestly completable inside judging if the demo wallet holds $5 of MUx from ~27 Sep and the cron runs daily through 2 Oct.

**M14. `.env.example` is gitignored — a clone has no env template** *(requirements, code; verified)*
`git check-ignore -v .env.example` → `.gitignore:34 .env*`; the file exists on disk (complete: every `env.ts` key + `NEXT_PUBLIC_APP_NAME`) but is untracked. `env.ts:8,14,15` hard-require `DATABASE_URL`, `JWT_SECRET` (≥ 32), `CRON_SECRET` and throw on the first API call. HANDOFF §4.3 lists the vars, so "cannot be discovered" is overstated, but the README's `cp .env.example .env.local` step would fail. **Fix (Claude, 0.1 h Tue):** add `!.env.example` directly after line 34, delete the redundant block at lines 43–46, `git add .env.example`. Content tweaks: `NEXT_PUBLIC_RPC` placeholder `https://mainnet.helius-rpc.com/?api-key=` is a broken URL when the key is empty (→ `https://api.mainnet-beta.solana.com`); `PYTH_API_KEY` is optional (Jupiter carries settlement) and the sign-up URL is `https://pythdata.app/signup`; `DATABASE_URL` comment → "Supabase: transaction pooler (6543) with `?pgbouncer=true`"; `DIRECT_URL` → session pooler 5432; make the badge-wallet funding figure agree with the README (0.3 SOL).

**M15. `docs/HANDOFF.md` (tracked, public on push) contradicts the shipped product and announces ported code that was never ported** *(requirements, code, story; verified)*
Covered by M6's HANDOFF banner + line fixes. The disclosure premise ("ported from earlier projects") is unsupported by the repo: `<path>` placeholders at lines 238/253 never filled, zero references in src/tests, `parimutuel.ts` and `calls.ts` are fresh modules. Founder must confirm before any wording ships (G11); a false disclosure is worse than none.

### LOW — polish

Consolidated from all lenses; fold into Thursday's P5 pass in this order of value.

| # | Area | Finding | Fix | h |
|---|---|---|---|---|
| L1 | Submission form | No `docs/SUBMISSION.md`; the one draft overclaims (Kamino/Jupiter Plays are coming-soon, `/admin/attribution` not built, Badges unminted, "live Jupiter/Pyth prices", "MIT" before LICENSE exists) | Write it Thursday with corrected claims; reuse `POSITIONING` as the one-liner; main track only, do not tick DBC | 0.75 |
| L2 | Dependencies | `@solana/spl-token-metadata` imported (`mint.ts:43`) but undeclared; hoisted from `@solana/spl-token` 0.4.15 so `npm ci` reproduces it; `shadcn` CLI in `dependencies` (harmless) | Founder: `npm i @solana/spl-token-metadata@0.1.6`; move `shadcn` to devDependencies; add `engines.node >=20` | 0.2 |
| L3 | Pyth key | Every chip says Jupiter; a key would not change settlement (see M6) | Fix copy; do not buy a key | 0 |
| L4 | Social preview | No OG/Twitter image; `layout.tsx:53-64` `twitter.card = "summary"`. The X post attaches native video so no card renders there; bites on bare-link shares | `src/app/opengraph-image.tsx` (next/og `ImageResponse`, 1200×630, `#0b0b0f`, ember tamga from `TAMGA_PATHS`, headline, "Plays · League · Calls · Mirror · Badges — Season 0 on Solana", dulo.fun); `twitter.card = "summary_large_image"`; verify at `/opengraph-image` | 0.75 |
| L5 | Attribution | `/admin/attribution` missing (HANDOFF §3.6). Per-partner completions already public on `/partners` | `src/app/admin/attribution/route.ts` GET behind `assertCronSecret`; `getAttribution(seasonId)` = Plays by campaign/partner, PlayProgress complete counts + unique users, PointsEvent `play:<key>` sums; `cache-control: no-store`; test a pure aggregation helper (db is mocked as `{}` in `tests/queries.test.ts`); skip CSV and `ADMIN_SECRET` | 1.5 |
| L6 | Error pages | No `not-found.tsx`/`error.tsx`; `/api/v1/<unknown>` returns HTML. No in-app link reaches a 404; every page has `ErrorState` + Retry | `not-found.tsx` (tamga, "Nothing here", back to Plays), `error.tsx` (`"use client"`, `{error, reset}`), `api/v1/[...rest]/route.ts` exporting GET/POST/PUT/PATCH/DELETE → `{ok:false}` 404 | 0.5 |
| L7 | Tap targets | Proof 55×28, Place Call 96×28, leaderboard Mirror 53×18, copy/explorer icons 16 px 20 px apart (only true WCAG AA failure) | `h-10 md:h-8` on PlayCard/MarketCard/MirrorPlanCard buttons (idiom exists at `ConnectButton.tsx:29`); negative-margin hit area on the Mirror link (`-my-2 py-2 px-2`); icons `size-7 -m-1.5` with `size-3` glyphs | 1 |
| L8 | Video script | HANDOFF §6 stale ("Thirty apps", "Pyth settles", "Badge lands in wallet", shots needing prod state) | Replace with the shot list from the story lens (T6); superseded 15 Sep by the pitch (§6) and technical (§6b) scripts | 0.5 |
| L9 | SIWS host trust | `api.ts:103-122` derive domain/URI from `x-forwarded-host`/`proto` with no allowlist; AuthNonce not bound to domain/address. Mitigated on Vercel (`x-forwarded-host` = Host, routed) | Accept the header host only when it equals `NEXT_PUBLIC_APP_URL`'s host or `VERCEL_URL`/`VERCEL_BRANCH_URL`/`VERCEL_PROJECT_PRODUCTION_URL` (localhost in dev), else fall back; do **not** allowlist `*.vercel.app` (reopens the hole); extend `tests/siws.test.ts:474` | 1 |
| L10 | Cron secret | `?secret=` accepted; `!==` compare; schema allows 1 char; no unit test | Header only in production; `timingSafeEqual`; `.min(16)`; 10-line test | 0.3 |
| L11 | Security headers | None on a wallet-signing app | `headers()` in `next.config.ts`: `frame-ancestors 'none'`, `X-Frame-Options: DENY`, nosniff, `Referrer-Policy: strict-origin-when-cross-origin`, HSTS; keep badge routes' CORS | 0.3 |
| L12 | RPC URL leak | `${rpcUrl}` (embeds the Helius key) interpolated into an error that can reach API responses | Report `new URL(rpcUrl).host` only; grep for other interpolations | 0.1 |
| L13 | Tick health | Stale `maxDuration` comment; no health fields for pingers | `maxDuration = 300`; include `catalogueOrigin`, `slotAnchorAgeMs`, `snapshot.failed.length`, `badges.pending/failed` in the tick JSON; pinger runs `jq -e '.data.steps \| all(.ok)'` | 0.3 |
| L14 | next-env.d.ts | Gitignored, so `npm run typecheck` fails on a fresh clone until Next generates it | README: `npm run dev` or `npx next typegen` before `typecheck` | 0.1 |
| L15 | CI | No workflow; 506 tests invisible | `.github/workflows/ci.yml` (node 22, `npm ci`, lint, `next typegen` + typecheck, test; dummy `JWT_SECRET`/`CRON_SECRET`/`DATABASE_URL` as in `tests/siws.test.ts`) + README badge | 0.5 |
| L16 | Public-safety | Tracked files clean — safe to push | Re-run the grep + `git status --ignored` before the first push; never `git add -f .env.local`; README note that bot wallets are deterministic and unfunded by design | 0.1 |
| L17 | Mobile trade sheet | CTA clips below the fold at 375×812 | `sticky bottom-0 bg-background pt-2 pb-[env(safe-area-inset-bottom)]` on the footer inside `SheetContent`; trim the helper line | 0.5 |
| L18 | Wallet connect | Choosing Phantom without the extension closes the modal silently | Watch `wallet?.readyState === NotDetected` after select → toast "Phantom not found — install at phantom.app or open Dulo in the wallet app" | 0.5 |
| L19 | Tab titles | Partner page title "Xstocks" (from slug); `/calls` has no title | `generateMetadata` via `getPartner(slug)` with `titleFromSlug` fallback; `src/app/calls/layout.tsx` with `metadata = { title: "Calls" }` | 0.3 |
| L20 | Proof drawer | Signed-out state has no Connect action | Render `<ConnectButton size="default" />` under the message | 0.3 |
| L21 | Accessibility | Two `nav` landmarks share `aria-label="Primary"` | Tab bar → "Primary, mobile" | 0.1 |
| L22 | Calendar / DST | League `weekEnd` fixed 20:00 UTC while Calls use the ET calendar — diverge after 1 Nov; calendar runway ends Jan 2027 | Post-judging: derive `weekEnd` from `nextMarketClose` of Friday in `weekContaining` and `seed.ts leagueWeekFor`; add 2027 holidays | 0.5 |
| L23 | Leaderboard emptiness | With bots excluded (H2) the Season board and `/mirror` on-chain list are empty until real wallets score | Founder: sign in 3–5 own wallets holding ≥ $5 of three xStocks across two sectors before Friday; one real Jupiter swap from one of them gives an on-chain Mirror target. Zero code | 0.5 |
| L24 | Tests | All DB logic behind `vi.mock`; no integration test for locks/groupBy/P2002 | Post-judging: `tests/integration/db.test.ts` gated on `DATABASE_URL`; `npm run test:db` | 2.5 |
| L25 | Duplication | `truncateAddress` ×2, `formatCountdown` ×2, three USD formatter families | Consolidate into `components/common/format.ts`; ~12 import sites | 0.5 |
| L26 | API consistency | Inconsistent `runtime`/`dynamic` exports and `cache-control` on personalised GETs | Shared 4-line snippet; `NO_STORE` constant from `api.ts`; pass it from every `getSession()` handler | 0.3 |
| L27 | Code organisation | `queries.ts` is 728 lines across six domains | Post-judging: split into `queries/{season,plays,leaderboard,partners,profile,mirror}.ts` with an index re-export | 1 |
| L28 | X post | Draft kept in the private planning notes | Post Friday with the video attached natively | 0.25 |
| L29 | Earnings Holder in window | See M13 — hold $5 of MUx by ~27 Sep, NKEx/ACNx by ~28 Sep | Founder; zero code | 0.25 |
| L30 | HANDOFF day plan | §2/§5 drift; unticked Mon/Tue items | Part of the M6 banner; tick §5 as done | 0.5 |
| L31 | Data hygiene | A non-bot League account already sits on the local board | Deploy to a fresh Supabase and seed there; do not copy the local DB; sign in the demo wallet only after `HELIUS_API_KEY` is set so its first snapshot is real | 0.25 |
| L32 | Mirror Play desc | Can be read as in-app execution | Covered by M6 | 0 |

### Explicitly drop

- **Blink / `actions.json`** and everything on HANDOFF's "only if ahead" list — zero judge value against README, video and deploy.
- **Jupiter Trigger/Recurring-verified partner Play** — needs a new event source and an API key that does not exist; the Recurring API is marked unmaintained.
- **Real on-chain players ("unclaimed" top holder wallets) on the board** — 8 h, and it touches the leaderboard semantics the week of submission; L23's five own wallets get most of the effect.
- **`User.isBot` schema migration** — `LeagueAccount.isBot` already exists; use it.
- **Stacked-card mobile tables** — className changes (M9) are enough this week.
- **Buying a Pyth key** — settlement would still be Jupiter (M6).
- **cron-job.org as a second pinger** — 30 s client timeout logs long ticks as failures; Pro plan or Actions suffices.
- **Live wallet/points counters on the landing** — they would read 0–1 for two weeks.
- **A competitive "xPoints vs Dulo" line on the landing hero** — sponsor optics; keep it complementary and in README/video.
- **Deleting the `.claude/worktrees` branch before pushing** — already excluded; a single-branch push ignores it; removal could break the other session.
- **Post-judging items** (L22, L24, L27, per-wallet snapshot dedupe beyond the empty-row case).

